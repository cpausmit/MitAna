# arguments, output, error, log, universe, executable, getenv, input, should_transfer_files, when_to_transfer_output are forced to specific values in runOnDatasets.py
requirements            = UidDomain == "mit.edu" && Arch == "X86_64" && HasFileTransfer && OpSysAndVer == "SL6"
notification            = Error
rank                    = Mips
transfer_output_files   = {fileset}.root
#transfer_output_remaps  = "{fileset}.root = /mnt/hscratch/{task}/{book}/{dataset}/{fileset}.root"
